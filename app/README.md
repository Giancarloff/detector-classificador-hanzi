# APP com a aplicação do modelo


### Utilize uma a env Anaconda para editar o app, para isso, abra esta pasta e execute o código abaixo no terminal:

```bash
conda env create -f environment.yml
```

```sh
conda activate kivy_torch_env
``` 